import 'package:flutter/material.dart';

class BranchModel{
  IconData branchIcon;
  String name;
  String address;
  String distance;
  String price;

  BranchModel({this.branchIcon, this.name, this.address, this.distance, this.price});
}