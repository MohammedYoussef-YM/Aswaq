import 'package:flutter/material.dart';

class AddressModel{
  IconData iconData;
  String name;
  String address;
  double latitude;
  double longitude;

  AddressModel({this.iconData, this.name, this.address, this.latitude, this.longitude});
}